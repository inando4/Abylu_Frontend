/**
 * Configuración centralizada de la API.
 *
 * Equivalente a tener spring.datasource.url en application.properties.
 * Si mañana tu backend cambia de puerto o dominio, solo cambias aquí.
 */
export const API_BASE_URL = 'http://localhost:8090/api';
